# pdf.yazi

Minimal PDF image preview for yazi: one page per peek via pdftoppm, shown with
ya.image_show, cached per page in yazi own file-cache. Meta strip (mime, size,
mtime) + Page N/M counter on the bottom row.

Requires: poppler (pdfinfo, pdftoppm).
